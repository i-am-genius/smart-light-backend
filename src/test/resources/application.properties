# Test-only configuration for full Spring application context tests.
# Production JWT and AI endpoint configuration must continue to come from deployment configuration/environment.
jwt.secret=smart-light-test-secret-at-least-32-chars-2026
jwt.expire-millis=3600000

# Syntactically valid local endpoints; context tests do not invoke these services.
ai.fabric.url=http://127.0.0.1:18081/fabric-recognize
ai.flow.url=http://127.0.0.1:18082/person-detect
